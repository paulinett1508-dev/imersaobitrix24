{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'INPUT=$(cat); if echo \"$INPUT\" | jq -r \".tool_input.command\" | grep -q \"git commit\"; then cd /c/PROJETOS/imersaobitrix24 && git push 2>/dev/null || true; fi'",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
